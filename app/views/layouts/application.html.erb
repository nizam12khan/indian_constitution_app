<!DOCTYPE html>
<html>
  <head>
    <title>Indian Constitution</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-gray-100 font-sans min-h-screen">
    <header class="header">
      <div class="container flex flex-col sm:flex-row justify-between items-center">
        <h1 class="text-2xl font-bold mb-2 sm:mb-0">Indian Constitution</h1>
        <div>
          <%= form_with url: search_path, method: :get, data: { turbo_frame: "search_results" }, class: "flex" do %>
            <%= text_field_tag :query, nil, placeholder: "Search article number or keyword", class: "p-2 rounded-l text-black w-full sm:w-64 focus:outline-none focus:ring-2 focus:ring-blue-300" %>
            <%= submit_tag "Search", class: "bg-blue-800 text-white p-2 rounded-r hover:bg-blue-900 transition" %>
          <% end %>
        </div>
      </div>
    </header>
    <main class="container">
      <%= yield %>
      <turbo-frame id="search_results" class="mt-4"></turbo-frame>
    </main>
  </body>
</html>